absl-py==1.4.0  # Compatible, no change needed
astunparse==1.6.3  # Compatible, no change needed
cachetools==5.3.1  # Compatible, no change needed
certifi==2023.5.7  # Compatible, no change needed
charset-normalizer==3.1.0  # Compatible, no change needed
contourpy==1.0.7  # Compatible, no change needed
cycler==0.11.0  # Compatible, no change needed
flatbuffers==23.5.26  # Compatible, no change needed
fonttools==4.39.4  # Compatible, no change needed
gast==0.5.4  # Compatible, no change needed
google-auth==2.19.0  # Compatible, no change needed
google-auth-oauthlib==0.4.6  # Compatible, no change needed
google-pasta==0.2.0  # Compatible, no change needed
grpcio==1.54.2  # Compatible, no change needed
h5py==3.8.0  # Compatible, no change needed
idna==3.4  # Compatible, no change needed
importlib-metadata==6.6.0  # Compatible, no change needed
importlib-resources==5.12.0  # Compatible, no change needed
keras==2.17.0  # Update to match TensorFlow 2.17.0
Keras-Preprocessing==1.1.2  # Compatible, no change needed
kiwisolver==1.4.4  # Compatible, no change needed
libclang==16.0.0  # Compatible, no change needed
Markdown==3.4.3  # Compatible, no change needed
MarkupSafe==2.1.2  # Compatible, no change needed
matplotlib==3.7.1  # Compatible, no change needed
numpy==1.24.3  # Compatible, no change needed
oauthlib==3.2.2  # Compatible, no change needed
opt-einsum==3.3.0  # Compatible, no change needed
packaging==23.1  # Compatible, no change needed
Pillow==9.5.0  # Compatible, no change needed
protobuf==3.20.3  # Update for TensorFlow 2.17.0
pyasn1==0.5.0  # Compatible, no change needed
pyasn1-modules==0.3.0  # Compatible, no change needed
pyparsing==3.0.9  # Compatible, no change needed
python-dateutil==2.8.2  # Compatible, no change needed
requests==2.31.0  # Compatible, no change needed
requests-oauthlib==1.3.1  # Compatible, no change needed
rsa==4.9  # Compatible, no change needed
six==1.16.0  # Compatible, no change needed


tensorboard-plugin-wit==1.8.1  # Compatible, no change needed
tensorflow==2.17.0  # Update to TensorFlow 2.17.0

tensorflow-estimator==2.17.0  # Update to match TensorFlow 2.17.0

termcolor==2.3.0  # Compatible, no change needed
tfimm==0.2.14  # Compatible, no change needed
typeguard==4.0.0  # Compatible, no change needed
typing_extensions==4.6.2  # Compatible, no change needed
urllib3==1.26.16  # Compatible, no change needed
Werkzeug==2.3.4  # Compatible, no change needed
wrapt==1.15.0  # Compatible, no change needed
zipp==3.15.0  # Compatible, no change needed
